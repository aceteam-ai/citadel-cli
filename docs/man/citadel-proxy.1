.nh
.TH "CITADEL" "1" "Jan 2026" "AceTeam" "Citadel CLI Manual"

.SH NAME
citadel-proxy - Forward a local port to a service on another node


.SH SYNOPSIS
\fBcitadel proxy [local-port] [peer:port] [flags]\fP


.SH DESCRIPTION
Creates a local TCP proxy that forwards connections to a service on another node.

.PP
This allows you to access services on remote peers using localhost. Any connection
to the local port will be forwarded through the AceTeam Network to the remote peer.

.PP
PEER IDENTIFICATION:
  You can specify the peer in multiple ways:
  - By hostname:  citadel proxy 8080 gpu-node-1:11434
  - By IP:        citadel proxy 8080 100.64.0.25:11434
  - Interactive:  citadel proxy  (prompts for peer and ports)

.PP
COMMON SERVICES:
  - Ollama:    port 11434
  - vLLM:      port 8000
  - llama.cpp: port 8080
  - PostgreSQL: port 5432
  - Redis:     port 6379

.PP
The proxy runs until interrupted (Ctrl+C).


.SH OPTIONS
\fB--bind\fP="127.0.0.1"
	Address to bind to (default localhost only)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for proxy

.PP
\fB--max-conns\fP=0
	Maximum concurrent connections (0 = unlimited)

.PP
\fB-v\fP, \fB--verbose\fP[=false]
	Show connection activity


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--auth-service\fP="https://aceteam.ai"
	The URL of the authentication service

.PP
\fB--config\fP=""
	config file (default is $HOME/.citadel-cli.yaml)

.PP
\fB--debug\fP[=false]
	Enable debug output

.PP
\fB--nexus\fP="https://nexus.aceteam.ai"
	The URL of the AceTeam Nexus server


.SH EXAMPLE
.EX
  # Interactive mode - guided setup
  citadel proxy

  # Forward localhost:8080 to Ollama on gpu-node-1
  citadel proxy 8080 gpu-node-1:11434
  # Then access via: curl http://localhost:8080/v1/models

  # Forward localhost:5432 to PostgreSQL
  citadel proxy 5432 db-server:5432

  # Forward with verbose logging
  citadel proxy 8080 gpu-node-1:11434 --verbose

  # Bind to all interfaces (not just localhost)
  citadel proxy 8080 gpu-node-1:11434 --bind 0.0.0.0
.EE


.SH SEE ALSO
\fBcitadel(1)\fP
